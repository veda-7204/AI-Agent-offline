# Code Review Report

**File:** `sample_buggy_code.py`
**Event:** Created
**Date:** 2025-04-06 14:30:22
**Lines of code:** 20
**Model:** CodeLlama (Ollama — offline)

---

## Code Reviewed

```python
def calculate_discount(price, discount_percent):
    discounted = price - (price * discount_percent / 100)
    return discounted

def get_user_data(user_id):
    users = {1: "Alice", 2: "Bob", 3: "Charlie"}
    return users[user_id]

def divide(a, b):
    return a / b

items = [10, 20, 30, 40, 50]
total = 0
for i in range(len(items) + 1):
    total += items[i]

print(calculate_discount(100, 20))
print(get_user_data(5))
print(divide(10, 0))
print(total)
```

---

## AI Analysis

### 1. SUMMARY OF CHANGES

This file introduces four utility operations: a discount calculator, a user data lookup by ID, a division helper, and a list summation loop. It appears to be an early draft or a script written quickly without error handling. The code covers basic arithmetic and dictionary lookups, and attempts to sum a list of integers using an index-based loop. All four operations are immediately called via `print` statements at the bottom of the file.

---

### 2. SUGGESTIONS

- **Use `dict.get()` for safe lookups** — replace `users[user_id]` with `users.get(user_id, "User not found")` so missing keys return a default value instead of crashing.

- **Add input validation to `calculate_discount`** — negative prices or discount values above 100 would produce nonsensical results. Add a guard at the top of the function.

- **Prefer direct iteration over index-based loops** — instead of `for i in range(len(items))`, use `total = sum(items)` or `for item in items: total += item`. It's cleaner, more Pythonic, and eliminates the risk of index errors.

- **Add type hints** — functions like `calculate_discount(price: float, discount_percent: float) -> float` make the code self-documenting and easier to maintain.

- **Separate script logic from function definitions** — wrap the `print` calls at the bottom in an `if __name__ == "__main__":` block so the functions can be safely imported elsewhere without side effects.

- **Add docstrings** — each function should have a short description of what it does, its parameters, and its return value.

---

### 3. ERRORS & BUGS DETECTED

**[CRITICAL] Line 14 — IndexError: list index out of range**
```python
for i in range(len(items) + 1):  # range goes 0 to 5, but list only has indices 0 to 4
    total += items[i]             # items[5] does not exist — crashes at runtime
```
Fix: change `len(items) + 1` to `len(items)`.

---

**[CRITICAL] Line 19 — ZeroDivisionError**
```python
print(divide(10, 0))  # Dividing by zero raises ZeroDivisionError at runtime
```
Fix: add a zero-check inside `divide()`:
```python
def divide(a, b):
    if b == 0:
        return "Error: division by zero"
    return a / b
```

---

**[CRITICAL] Line 18 — KeyError: 5**
```python
print(get_user_data(5))  # user_id=5 does not exist in the dictionary
```
Fix: use `users.get(user_id, "Unknown user")` instead of `users[user_id]`.

---

**[WARNING] No input validation on `calculate_discount`**
A `discount_percent` of 150 would return a negative price. Not a crash, but a logical error that would silently produce wrong results in production.

---

### 4. OVERALL RATING

**4 / 10** — The logic is understandable and the intent is clear, but all three `print` calls at the bottom will crash at runtime due to unhandled errors. The code needs input validation, safe dictionary access, and a corrected loop bound before it is production-ready.

---

*Report generated by Offline AI Code Review Agent using CodeLlama via Ollama.*
